<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;
use App\Models\Spr\IcdGroup;

use DB;


class TestController extends Controller
{
   

        /**
     * Create a new view for creating a new product in storage.
     *
     * @param  ...
     * @return \Illuminate\Http\Response
     */              
    public function index()
    {
       // $rt=DB::connection('admin')->select("select * from Patient WHERE PatientBirthDate='1960-04-27 00:00:00.000' "); s  dd($categories);SELECT * FROM `icd_group` where gr2='(A30-A49)'
       // dump($rt);
       // exit();

     `
    
        
    }
   }